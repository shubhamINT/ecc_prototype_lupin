version: '3.8'

services:
  ecc-webapp:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ecc-web-prototype
    ports:
      - "3000:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production